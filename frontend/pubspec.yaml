name: senior_job_app
description: GoWork 시니어 구인구직 AI 앱
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  provider: ^6.1.1
  uuid: ^4.5.3
  url_launcher: ^6.3.2

flutter:
  uses-material-design: true
